[project]
name = "book-exchange-prototype"
version = "0.1.0"
description = "FastAPI prototype for a book club exchange app"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "python-multipart>=0.0.30",
    "sqlite-web>=0.7.2",
    "sqlmodel>=0.0.22",
    "uvicorn>=0.30.0",
]
